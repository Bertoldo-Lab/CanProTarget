# This project does not use renv; it runs from the system library. renv was
# removed rather than left disabled, because a lockfile present but unused
# breaks the shinyapps deploy and misleads anyone reading the repo.
options(repos = c(
  CRAN = "https://cran.rstudio.com/"
))
