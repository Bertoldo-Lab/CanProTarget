# Logs directory

- **`usage/`**: created automatically when `CPT_LOG_USAGE=1`. Contains `usage_YYYY-MM-DD.jsonl` (gitignored).
- Do not commit large log files; they are excluded via `.gitignore`.

See **`docs/LOGGING.md`** for configuration and privacy notes.
